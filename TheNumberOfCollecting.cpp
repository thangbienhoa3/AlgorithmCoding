#include <iostream>
#include <algorithm>
#include <map>

using namespace std;

typedef long long int lld;

int main(){
    lld n;
    cin >> n;
    lld a[n+1];
    for(int i = 0;i < n;i++){
        cin >> a[i];
    }    
    
}